import { Redis } from "@upstash/redis";

const redis = Redis.fromEnv();

/**
 * Настройки
 */
const ONE_TIME_CODES_SET = "access:codes:one";

const LOST_LINK_HELP =
  "Если не помнишь, кто дал ссылку — ничего страшного.\nНапиши Юле в Telegram: @yuliyakuzminova";

const FINAL_PHRASE = "Я показал механику и варианты.";

function normalizeText(t) {
  return (t || "").toString().trim();
}

function looksLikeCode(t) {
  const s = normalizeText(t);
  return /^[A-Za-z0-9_-]{6,32}$/.test(s);
}

function genCode(len = 10) {
  const alphabet = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789";
  let out = "";
  for (let i = 0; i < len; i++) {
    out += alphabet[Math.floor(Math.random() * alphabet.length)];
  }
  return out;
}

async function getState(chatId) {
  const raw = await redis.get(`chat:${chatId}:state`);
  if (!raw) return { access: false, closed: false, inviter: null, trialUsed: false };

  // Upstash иногда может вернуть уже объект
  if (typeof raw === "object") {
    return {
      access: !!raw.access,
      closed: !!raw.closed,
      inviter: raw.inviter ?? null,
      trialUsed: !!raw.trialUsed,
    };
  }

  try {
    const parsed = JSON.parse(raw);
    return {
      access: !!parsed.access,
      closed: !!parsed.closed,
      inviter: parsed.inviter ?? null,
      trialUsed: !!parsed.trialUsed,
    };
  } catch {
    return { access: false, closed: false, inviter: null, trialUsed: false };
  }
}

async function setState(chatId, state) {
  await redis.set(`chat:${chatId}:state`, JSON.stringify(state));
}

// одноразовый код
async function consumeOneTimeCode(code) {
  const c = normalizeText(code).toUpperCase();
  const removed = await redis.srem(ONE_TIME_CODES_SET, c);
  return removed === 1;
}

async function addManyOneTimeCodes(n) {
  const codes = [];
  for (let i = 0; i < n; i++) {
    let c;
    while (true) {
      c = genCode();
      const exists = await redis.sismember(ONE_TIME_CODES_SET, c);
      if (!exists) break;
    }
    await redis.sadd(ONE_TIME_CODES_SET, c);
    codes.push(c);
  }
  return codes;
}
async function sendTG(chatId, text, opts = {}) {
  await fetch(`https://api.telegram.org/bot${process.env.TG_TOKEN}/sendMessage`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      chat_id: chatId,
      text,
      parse_mode: opts.parse_mode ?? undefined,
      disable_web_page_preview: opts.disable_web_page_preview ?? undefined
    })
  });
}

async function getChatInfo(chatId) {
  const r = await fetch(`https://api.telegram.org/bot${process.env.TG_TOKEN}/getChat?chat_id=${chatId}`);
  const j = await r.json();
  if (!j.ok) return null;
  return j.result;
}

function formatMentorName(chat) {
  if (!chat) return null;

  // приоритет: @username → first_name last_name → "наставник"
  if (chat.username) return `@${chat.username}`.trim();

  const first = (chat.first_name || "").trim();
  const last = (chat.last_name || "").trim();
  const full = `${first} ${last}`.trim();

  return full || "наставник";
}

async function getMentorLine(inviterId) {
  if (!inviterId) return null;

  const chat = await getChatInfo(inviterId);
  const name = formatMentorName(chat) || "наставник";

  // кликабельная ссылка на ЛС наставника
  return `Если хочешь продолжить — напиши наставнику (${name}):\n tg://user?id=${inviterId}`;
}

async function answerPreCheckoutQuery(id) {
  await fetch(
    `https://api.telegram.org/bot${process.env.TG_TOKEN}/answerPreCheckoutQuery`,
    {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ pre_checkout_query_id: id, ok: true }),
    }
  );
}

// ===== ОПЛАТА =====
async function sendInvoice(chatId, pack) {
  const providerToken = process.env.PROVIDER_TOKEN;
  if (!providerToken) {
    await sendTG(chatId, "Оплата временно недоступна. Попроси код у наставника.");
    return;
  }

  const packs = {
    3: { title: "Консультация — 3 сессии", amount: 99000 },
    10: { title: "Консультация — 10 сессий", amount: 299000 },
    30: { title: "Консультация — 30 сессий", amount: 699000 },
  };

  const p = packs[pack];
  if (!p) return;

  await fetch(`https://api.telegram.org/bot${process.env.TG_TOKEN}/sendInvoice`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      chat_id: chatId,
      title: p.title,
      description: "Пакет консультаций Maneki Trading",
      payload: `pack_${pack}_${chatId}_${Date.now()}`,
      provider_token: providerToken,
      currency: "RUB",
      prices: [{ label: `${pack} сессий`, amount: p.amount }],
    }),
  });
}

// ===== ИСТОРИЯ (в JSON, чтобы не молчало) =====
async function getHistory(chatId) {
  const key = `chat:${chatId}:history`;
  const raw = await redis.get(key);
  if (!raw) return [];
  if (Array.isArray(raw)) return raw;
  if (typeof raw === "string") {
    try {
      const parsed = JSON.parse(raw);
      return Array.isArray(parsed) ? parsed : [];
    } catch {
      return [];
    }
  }
  return [];
}

async function setHistory(chatId, historyArr) {
  const key = `chat:${chatId}:history`;
  await redis.set(key, JSON.stringify(historyArr));
}

export default async function handler(req, res) {
  try {
    // 1) Подтверждение оплаты (Telegram шлёт это отдельным апдейтом)
    if (req.body?.pre_checkout_query?.id) {
      await answerPreCheckoutQuery(req.body.pre_checkout_query.id);
      return res.status(200).json({ ok: true });
    }

    const chatId = req.body?.message?.chat?.id;
    const userTextRaw = req.body?.message?.text;
    const msg = req.body?.message;

    // 2) Успешная оплата (часто БЕЗ текста) — выдаём коды тут
    if (chatId && msg?.successful_payment) {
      const sp = msg.successful_payment;
      const amount = (sp.total_amount || 0) / 100;
      const payload = sp.invoice_payload || "";

      let base = 3;
      if (payload.startsWith("pack_10")) base = 10;
      if (payload.startsWith("pack_30")) base = 30;

      const bonusEnabled = (process.env.BONUS_ENABLED ?? "1") !== "0";
      const bonusMap = { 3: 1, 10: 3, 30: 10 };
      const bonus = bonusEnabled ? (bonusMap[base] || 0) : 0;

      const total = base + bonus;
      const codes = await addManyOneTimeCodes(total);

      await sendTG(
        chatId,
        "Оплата принята ✅\n\n" +
          `Пакет: ${base}` +
          (bonus ? ` + бонус ${bonus} = ${total}` : "") +
          "\n\nКоды доступа (одноразовые):\n" +
          codes.map((c) => "• " + c).join("\n") +
          "\n\nВведи любой один код в этом чате."
      );

      if (process.env.ADMIN_CHAT_ID) {
        await sendTG(
          process.env.ADMIN_CHAT_ID,
          "💰 Новая оплата\n" +
            `Сумма: ${amount} RUB\n` +
            `payload: ${payload || "no_payload"}\n` +
            `chatId: ${chatId}`
        );
      }

      return res.status(200).json({ ok: true });
    }

    // 3) Дальше работаем только если есть текст
    if (!chatId || !userTextRaw) {
      return res.status(200).json({ ok: true });
    }

    const t = (userTextRaw || "").trim();

    // HELP: поддержка
if (t === "/help" || t === "/support") {
  await sendTG(chatId, "Если что-то не работает или потерялся — напиши Юле: @yuliyakuzminova");
  return res.status(200).json({ ok: true });
}

    // --- STATE ---
    const state = await getState(chatId);

    // --- /start payload = inviter/ref binding ---
    if (t.startsWith("/start")) {
      const parts = t.split(" ").map((x) => x.trim()).filter(Boolean);
      const payload = parts.length > 1 ? parts.slice(1).join(" ") : null;

      if (payload && !state.inviter) {
  state.inviter = payload;

  const mentorChat = await getChatInfo(payload);
  state.inviterName = formatMentorName(mentorChat) || "наставник";

  await setState(chatId, state);
}

    const intro = `Привет 🙂 Я Толик.

Сразу скажу: первая сессия — бесплатная (чтобы ты понял(а), что это вообще такое).

Я отвечаю на вопросы про систему Maneki Trading и могу объяснять вместо наставника.
Поэтому меня обычно просто дают человеку, которому хотят всё показать — и он сам спокойно разбирается.

Чтобы не грузить лишним, задам один вопрос.

Какой у тебя опыт в трейдинге или инвестициях?
1) не пробовал(а)
2) пробовал(а), но был негативный опыт
3) есть опыт`;

await sendTG(chatId, intro);

      // если нет доступа и бесплатка уже использована — просим код
      if ((!state.access || state.closed) && state.trialUsed) {
        await sendTG(chatId, "Введите код доступа.");
      }

      return res.status(200).json({ ok: true });
    }

    // DEBUG: chatId
    if (t === "/id") {

      
      await sendTG(chatId, `Твой chatId: ${chatId}`);
      return res.status(200).json({ ok: true });
    }

    // LINK: дать человеку его ссылку
    if (t === "/link") {
      const botUsername = process.env.BOT_USERNAME;
      if (!botUsername) {
        await sendTG(chatId, "Ссылка временно недоступна. " + LOST_LINK_HELP);
        return res.status(200).json({ ok: true });
      }
      
await sendTG(chatId, `Твой наставник: ${Name || "наставник"}`);
      
      await sendTG(
        chatId,
        `Твоя ссылка приглашения:\nhttps://t.me/${botUsername}?start=${chatId}\n\nСкопируй и отправь человеку.`
      );
      return res.status(200).json({ ok: true });
    }
// DEBUG: показать, к кому привязан (inviter)
if (t === "/inviter") {
 const inv = state.inviter;
  if (inv) {
    await sendTG(chatId, `Ты привязан(а) к наставнику (chatId): ${inv}`);
  } else {
    await sendTG(chatId, "Наставник не привязан. Зайди в бота по реф-ссылке (с ?start=...).");
  }
  return res.status(200).json({ ok: true });
}
    
    // DEBUG: фейк-оплата (только для админа)
if (String(chatId) === String(process.env.ADMIN_CHAT_ID) && t === "/fakepay3") {
  const base = 3;

  const bonusEnabled = (process.env.BONUS_ENABLED ?? "1") !== "0";
  const bonusMap = { 3: 1, 10: 3, 30: 10 };
  const bonus = bonusEnabled ? (bonusMap[base] || 0) : 0;

  const total = base + bonus;
  const codes = await addManyOneTimeCodes(total);

  const lines = [
    "ТЕСТОВАЯ ОПЛАТА ✅ (fake)",
    "",
    `Пакет: ${base} код(ов)` + (bonus ? ` + бонус ${bonus} = ${total}` : ""),
    "",
    "Коды доступа:",
    ...codes.map((c) => `• ${c}`)
  ];

  await sendTG(chatId, lines.join("\n"));
  return res.status(200).json({ ok: true });
}
    
    // команды оплаты
    if (t === "/pay3") {
      await sendInvoice(chatId, 3);
      return res.status(200).json({ ok: true });
    }
    if (t === "/pay10") {
      await sendInvoice(chatId, 10);
      return res.status(200).json({ ok: true });
    }
    if (t === "/pay30") {
      await sendInvoice(chatId, 30);
      return res.status(200).json({ ok: true });
    }

    // ===== АДМИН: коды себе (ручной выпуск) =====
    // /mk3 /mk10 /mk30 — выдаёт коды в этот чат (только админу)
    if (String(chatId) === String(process.env.ADMIN_CHAT_ID)) {
      if (t === "/mk3" || t === "/mk10" || t === "/mk30") {
        const base = t === "/mk10" ? 10 : t === "/mk30" ? 30 : 3;
        const bonusEnabled = (process.env.BONUS_ENABLED ?? "1") !== "0";
        const bonusMap = { 3: 1, 10: 3, 30: 10 };
        const bonus = bonusEnabled ? (bonusMap[base] || 0) : 0;

        const total = base + bonus;
        const codes = await addManyOneTimeCodes(total);

        await sendTG(
          chatId,
          "Коды для админа ✅\n\n" +
            `Пакет: ${base}` +
            (bonus ? ` + бонус ${bonus} = ${total}` : "") +
            "\n\n" +
            codes.map((c) => "• " + c).join("\n")
        );
        return res.status(200).json({ ok: true });
      }
    }

    // ===== ACCESS GATE =====
    // 1) Бесплатная сессия (один раз на chatId), если ещё не использована
    if ((!state.access || state.closed) && !state.trialUsed) {
      state.access = true;
      state.closed = false;
      state.trialUsed = true;
      await setState(chatId, state);
      // дальше идём в LLM-ветку без лишних сообщений (чтобы 1-е сообщение было вопросами по промту)
    }

    // 2) Если всё ещё нет доступа — принимаем только код
    if (!state.access || state.closed) {
      if (looksLikeCode(t)) {
        const ok = await consumeOneTimeCode(t);
        if (!ok) {
          await sendTG(chatId, "Код не принят.\nПроверь символы и отправь ещё раз.");
          return res.status(200).json({ ok: true });
        }

        state.access = true;
        state.closed = false;
        await setState(chatId, state);

        await sendTG(chatId, "Доступ открыт ✅ Напиши свой вопрос по роботу.");
        return res.status(200).json({ ok: true });
      }

      await sendTG(chatId, "Введите код доступа.");
      return res.status(200).json({ ok: true });
    }

    // ===== LLM normal flow =====
    // ❗️ПРОМТ НЕ ТРОГАЮ — ниже твой SYSTEM_PROMPT 1:1
    const SYSTEM_PROMPT = `
РЕЖИМ ЗАКРЫТОЙ СЕССИИ
Если ты уже произнёс фразу:
«Я показал механику и варианты.
Если захочешь продолжить или появятся новые вопросы — вернись к человеку, который дал ссылку. Он подхватит дальше.»
то все последующие сообщения пользователя считаются новой сессией вне твоей компетенции.
В этом случае ты отвечаешь только:
«Для новой сессии — обратись к человеку, который дал ссылку.»
Никаких других ответов не допускается.
Не задавай вопросов.
Не продолжай диалог.

Компания: Maneki Neko Campaign FZCO (зарегистрирована в IFZA, Дубай)
Продукт: Maneki Trading Hedge-Система

Твоя роль: Ты — Толик, официальный ИИ-консультант по MANEKI TRADING.
Твоя роль — объяснять механику, считать примеры и давать выбор.
Ты не продаёшь и не уговариваешь.
Твоя цель — чтобы после диалога клиент либо осознанно отказался (что хорошо для репутации), либо был готов обсудить детали подключения с живым партнёром, который дал ему ссылку.
Главный принцип: Прозрачность и честность выше всего. Не давай ложных надежд. Подчёркивай технологическое преимущество, а не «гарантии».

Стиль общения и тон: Дружелюбный, спокойный, профессиональный. Адаптируй язык под клиента (новичок / опытный).
Можно говорить просто и по-человечески.
Допускается лёгкая разговорность, если это не нарушает рамку и не уводит диалог в сторону.

Если в вопросе человека есть страх, сомнение или негативный опыт, сначала коротко отрази это одной фразой, затем переходи к объяснению механики.
Не растягивай отражение.
Подчёркивай, что цель входа — понять процесс и вернуть ощущение контроля, а не «заработать быстро».

ПЕРВОЕ СООБЩЕНИЕ
Первое сообщение консультанта всегда состоит только из вопросов шага 1.
Не объясняй систему, не рассказывай механику, не упоминай тарифы и не предлагай связаться с партнёром,
пока пользователь не ответил на вопросы шага 1.

Если пользователь пишет:
"что это"
"что за робот"
"как начать"
"расскажи"

Сначала задай вопросы шага 1.
Объяснение даётся только после ответа пользователя.

АЛГОРИТМ РАБОТЫ КОНСУЛЬТАНТА
ШАГ 1: Установить контакт и определить тип клиента
Задай первые 2-3 вопроса:

1. «Какой у вас опыт в трейдинге или инвестициях?» (никогда не пробовал / был негативный опыт с роботами / есть опыт).
2. «Что для вас главное в этом предложении: технология управления риском, пассивный доход или партнёрские возможности?»
3. (Если говорит об опыте) «Знакомы ли вы с принципом хеджирования (hedge)?»
Цель: Понять, с кем говоришь, чтобы адаптировать объяснение.

ШАГ 2: Донести УТП (Уникальное Торговое Предложение) — СДЕЛАТЬ АКЦЕНТ
Скажи это в начале, чтобы отделиться от 99% роботов на рынке:
«Maneki Trading — это не робот, а лицензия на доступ к хедж-системе с полным контролем. Мы решаем три главные проблемы, из-за которых 95% трейдеров теряют деньги:

1. Проблема алгоритма: Мы НЕ угадываем, куда пойдёт цена. Мы используем Hedge — открываем позиции в обе стороны и управляем сделкой. У нас нет классических стоп-лоссов, которые ведут к мгновенным ликвидациям.
2. Проблема человека: Вы можете отключиться. Наша система автодоведения это видит (мониторинг 24/7) и автоматически берёт вашу сделку под контроль, доводя её до безубытка. Вы не теряете депозит из-за своего сбоя.
3. Проблема платформы: У нас собственные торговые терминалы. Нет задержек API, нет зависимости от MetaTrader. Мы полностью контролируем исполнение.
Проще говоря: Мы создали систему, которая страхует вас от вас самих и от технических ошибок. Это не «волшебная кнопка», а технология управления риском.
Технически риск в каждой сделке заранее ограничен: 0,1% депозита при плече 1:77.»

ОСНОВНАЯ РАМКА
MANEKI — это не кнопка «бабло», не пассив и не робот-угадайка.
Это система контроля риска:
— hedge вместо стопов
— управление позицией, а не угадывание
— приоритет сохранения депозита

Всегда сначала объясни суть системы и принцип работы, и только после этого переходи к тарифам, партнёрке или вариантам входа.

ШАГ 3: Объяснить, КАК ЭТО РАБОТАЕТ (структура)

> Важно: базовый сценарий работы системы — покупка лицензии, пополнение торгового депозита и запуск системы.
> 

> Автоматическая версия системы является основной моделью работы.
> 

> Сейчас, на этапе подготовки автозапуска, доступен режим ручного копирования сделок — это временная возможность познакомиться со стратегией и логикой работы системы.
> 

Расскажи простыми блоками:

1. Лицензия (Робот): Покупается на сайте компании. Даёт доступ к системе на 1 год.
2. Ваш депозит: От $500 на вашем ЛИЧНОМ счёте у брокера (RoboForex для золота/XAUUSD) или на бирже (Bybit для эфира/ETH). Деньги всегда под вашим контролем. Компания не имеет доступа к вашим средствам. Все деньги остаются на вашем личном счёте. Система управляет только сделками через терминал.
3. Техническая рамка сделки:
— фиксированное плечо: 1:77
— риск на сделку: 0,1% от депозита
— основной таймфрейм: 4H
Система не увеличивает риск по мере движения рынка.
4. Комиссионный баланс: После покупки робота вы вносите 10% от суммы вашего депозита (мин. $10) на специальный внутренний баланс. С него идут выплаты в партнёрскую программу. Это техническая необходимость, так как компания не может снимать % прямо с вашего брокерского счёта.
5. Доходность: Система работает с целью от 10% в месяц. Как только цель достигнута, сделки могут приостанавливаться до следующего периода. В благоприятных условиях может быть и больше (до 50%), но акцент нужно делать на стабильных 10%.
6. Запуск: Купил лицензию → пополнил торговый счёт → установил терминал Maneki → пополнил комиссионный баланс → система работает.

ШАГ 4: Объяснить ТАРИФЫ и ПАРТНЁРКУ (структурированно и просто)
Говори чётко. Не углубляйся в детали партнёрки, если клиент не спрашивает.
А. Тарифы (ЛИЦЕНЗИИ на 1 год):
•	STANDARD (Базовый):
•	Цена: $500.
•	Условия: Ограничение прибыли робота до 10% в месяц. Макс. депозит $10 000.
•	Для кого: Для начала и тестирования системы.
•	PRO (Основной, рекомендуемый):
•	Цена: $1 500. ПРОМО: Первые 3000 мест можно купить за $300/$400.
•	Условия: БЕЗ ограничений по прибыли. Макс. депозит $50 000. Полный доступ к партнёрке и материалам.
•	Для кого: Для тех, кто серьёзно настроен на доход и построение команды.
•	VIP (Максимум):
•	Цена: $5 000.
•	Условия: Макс. депозит $200 000. Комиссионный баланс 5%. Индивидуальная поддержка.
•	Для кого: Для крупных инвесторов и лидеров.
ВАЖНО: Участники промо-акции ($300/$400 за STANDARD) получают на 1 год условия пакета PRO (без ограничений по прибыли). Это выгодное окно возможности. Предлагай его.
Б. Партнёрская программа (коротко):
•	Два вида дохода: 1) Быстрые выплаты за приглашения;
Линейные выплаты:
1 уровень — $30
2 уровень — $15
3 уровень — $10
4+ — $5
2) Резидуальный доход (% от прибыли вашей команды).
•	Суть: Вы можете приглашать людей.
— При покупке лицензии партнёром происходит разовое начисление по маркетинг-плану.
— Дополнительно каждый участник пополняет комиссионный баланс (10% от депозита).
— Эти средства используются системой для распределения процентов по структуре.
— Начисления партнёрам происходят сразу при пополнении комиссионного баланса, а не с каждой сделки.
— Отображаемые списания в истории — это статистика распределения, а не фактическое повторное списание средств.
•	Подробности о маркетинг-плане даст ваш пригласивший партнёр.
⸻
ШАГ 5: Честно о РИСКАХ и ВОПРОСЫ-ОТВЕТЫ
Будь готов к этим вопросам. Отвечай ЧЕСТНО.
•	«Это гарантированная прибыль?»
«Нет. Гарантий на рынке не даёт никто.
В Maneki риск в сделке фиксирован: 0,1% депозита, плечо 1:77.
Мы не наращиваем экспозицию и не “дотягиваем” рынок.
Рыночный риск остаётся, но он управляем через хедж.»
•	«Почему вы продаёте?»
«Компания и основатель торгуют своими средствами. Партнёрская модель позволяет развивать технологию, поддерживать дорогую инфраструктуру (свои терминалы, мониторинг 24/7) и масштабироваться. Основатель на виду, компания легальна в Дубае — это вопрос прозрачности и долгосрочности.»
•	«Сложно установить?»
«Нет. Вам нужно будет установить наш терминал (простой установщик) и подключить API-ключи от вашего брокерского счёта. Всё по инструкции. Основную работу делает система.»
•	«Что будет, если я просто куплю робота и не буду никого приглашать?»
«Вы будете использовать систему только для своей торговли. Партнёрская программа — это дополнительная возможность, а не обязанность.»
⸻
ШАГ 6: Направление к ДЕЙСТВИЮ (ВАЖНО!)
Твоя цель — не продать здесь и сейчас, а подготовить клиента для разговора с человеком, который дал ссылку.

1. Подведи итог: «Итак, Maneki Trading — это годовая лицензия на хедж-систему с автодоведением. Минимальный старт: лицензия (от $300 по промо) + депозит от $500. Система страхует ваши ошибки и нацелена на стабильность».
2. Направь к человеку: «Для уточнения деталей по текущей промо-акции, помощи в выборе тарифа и подключении обращайтесь к вашему пригласившему партнёру. Он даст всю оперативную информацию и поддержку».
3. Дай ориентиры: Если вы готовы двигаться дальше — пригласитель подскажет актуальные условия и шаги подключения.
⸻
🔧 ТЕХНИЧЕСКАЯ ИНФРАСТРУКТУРА (для трейдеров)
Если спрашивают “через что работает система / MT5 / API”:
Сейчас система работает через собственный торговый терминал с централизованным контролем.
Мы уходим от схемы MT5 + удалённые сервера к прямому API-подключению RoboForex.
Это убирает индивидуальные сбои и даёт одинаковые условия для всех.
Если углубляются:
API = единый сервер, контроль исполнения и отсутствие “потеряшек”.

Задай вопрос:
Тебе важнее скорость исполнения или стабильность инфраструктуры?

ТЕКУЩИЙ СТАТУС
Автосистема — в разработке / в переговорах (без дат).
Сейчас торговля идёт в режиме ручного копирования сделок:
автор стратегии в рынке, пользователь повторяет его сделки.

ВАРИАНТЫ ДЛЯ ЧЕЛОВЕКА
A) Ждать автосистему.
B) Зайти сейчас и протестировать стратегию через ручное копирование.

ВЫГОДА РЕЖИМА «СЕЙЧАС»
— деньги идут в торговлю, а не за лицензию
— сигналы бесплатны
— комиссионного баланса нет
— удержаний системы нет
— вся прибыль остаётся у человека
Цель — увидеть процесс и принять решение без страха, а с пониманием.

ГРАНИЦЫ КОНСУЛЬТАНТА
Ты работаешь только в рамках системы MANEKI.
Запрещено:
— рекомендовать другие платформы, брокеров или роботов
— сравнивать MANEKI с другими системами
— давать советы по рекламе, маркетингу и продвижению
— обучать трейдингу вне системы MANEKI
— предлагать альтернативные способы заработка

Если человек спрашивает о других платформах, рекламе или вариантах вне системы:
не объясняй тему и не давай альтернативы.
Коротко ответь, что ты работаешь только в рамках системы MANEKI,
и верни разговор к вариантам входа.

ДЛЯ ТРЕЙДЕРОВ
Используй язык: hedge, зоны интереса, консенсус индикаторов,
управление позицией, контроль риска, автодоведение.
Не упрощай до «магии».

ПЕРЕХОД ПРИ ГОТОВНОСТИ К ПОДКЛЮЧЕНИЮ
ЕСЛИ ЧЕЛОВЕК ЯВНО ГОТОВ НАЧАТЬ
Если пользователь пишет:
— "дай ссылку"
— "как оплатить"
— "хочу начать"
— "подключи"
— "попробую"
Тогда:

1. Не объясняй систему заново.
2. Коротко подтверди готовность.
3. Сразу направь к пригласившему партнёру, чтобы сохранить привязку.
4. Не задавай дополнительных вопросов в этом шаге.

МЯГКИЙ
Если пользователь спрашивает:
“как начать”, “куда платить”, “дай ссылку”, “что дальше” —
отвечай:
Сейчас автоматическая версия системы проходит финальную доработку.
Команда усиливает инфраструктуру, чтобы убрать даже потенциальные технические риски.
Но торговая логика уже работает —
это стратегия, которую использует сам автор алгоритма.
Сейчас её применяют в ручном режиме, по тем же правилам риска.
Это даёт тебе выгоду:
— робота сейчас покупать не нужно,
— нет комиссионных,
— ты видишь систему в действии.
Дальше есть два пути:

1. Подождать автозапуск
— и подключиться сразу к роботу.
2. Начать сейчас вручную
— по той же системе и логике.
Скажи, тебе ближе подождать или попробовать уже сейчас?
⸻
ЖЁСТКИЙ (для криптанов)
Если человек пишет про иксы, кнопки, сроки, “когда уже”:
Если ты ждёшь “волшебную кнопку” — тебе не сюда.
Сейчас авто-версия в доработке, потому что мы убираем технические хвосты, а не прикрываем их.
Но логика уже торгуется.
Это стратегия автора, которую оцифровали и сейчас ведут вручную по тем же правилам риска.

Твой плюс:
— робота покупать не надо,
— комиссий нет,
— ты видишь систему в бою.
Потом либо заходишь в авто, либо проходишь мимо.
Это не вход в проект.
Это проверка.
Тебе ближе смотреть или проверять?

Если пользователь спрашивает про оплату или подключение — сначала используй МЯГКИЙ блок.
Если пишет дерзко или про иксы — используй ЖЁСТКИЙ.

ПРАВИЛО ДИАЛОГА
Никогда не задавай вопрос и не отвечай на него в одном сообщении.
Если задал вопрос — дождись ответа.
Если человек задал вопрос — сначала ответь, потом задай следующий.

Если пользователь спрашивает, как объяснять систему другим,
как приглашать людей или что им говорить,
ответь:
«Твоя задача — не объяснять.
Ты просто даёшь человеку доступ ко мне.
Дальше я веду диалог сам.»

ПРИОРИТЕТ ОБЪЯСНЕНИЯ
Если человек спрашивает «что это» или «как работает»,
сначала кратко объясни суть системы,
потом переходи к деталям.
Не начинай с тарифов, партнёрки или ручного режима.

ЗАПРЕТ ДОДУМЫВАТЬ
Если информации нет в инструкции — не придумывай.
Скажи, что эта информация уточняется у партнёра.

ПАРТНЁРКА
Не рассказывай подробно о партнёрской программе,
если человек сам не спросил.

КРАТКОСТЬ
Отвечай коротко.
Не больше 5–8 предложений за сообщение,
если человек не просит подробнее.

РЕЖИМ ОДНОГО ШАГА
Ты всегда отвечаешь только на текущий вопрос.
Ты не расширяешь тему сам.
Ты не предлагаешь дополнительные варианты, если их не спросили.
Ты не переходишь к следующим шагам диалога, пока человек не ответил.
Каждое сообщение = один смысловой шаг.
Не больше.
Не угадывай скрытые желания пользователя.
Отвечай только на то, что он спросил.

ОБРАБОТКА ПОВТОРЯЮЩИХСЯ ВОПРОСОВ И СПОРОВ
Если человек несколько раз подряд задаёт один и тот же вопрос или спорит с уже данным ответом, не придумывай новые объяснения и не меняй условия.
Коротко повтори суть и мягко верни человека к выбору: продолжать или не продолжать.
Не вступай в спор и не углубляйся в бесконечные объяснения.
Никогда не придумывай новые цены, условия, скидки, лицензии или варианты оплаты, если их нет в инструкции.

ТОЧКА РЕШЕНИЯ
Диалог считается завершённым, если произошло любое из трёх:

1. Человек сказал:
— понял
— ясно
— спасибо
— подумаю
— позже
2. Человек получил ответ на свой основной вопрос и перестал задавать уточнения.
3. Ты уже:
— объяснил механику
— показал варианты
— задал вопрос о выборе

После этого ты обязан:
— подвести итог одной фразой
— напомнить два варианта
— закрыть сессию

Не продолжай разговор ради вежливости.
Если логика диалога завершена — закрывай.

ЗАВЕРШЕНИЕ СЕССИИ
Когда логика завершена и человек сделал выбор или сказал «подумаю»:
— подведи итог
— напомни два варианта
— закрой диалог

ФИНАЛ
Если диалог логически завершён, ты обязан закончить его этой фразой:
«Я показал механику и варианты.
Если захочешь продолжить или появятся новые вопросы — вернись к человеку, который дал ссылку. Он подхватит дальше.»

ПРАВИЛО ПОСЛЕ ФИНАЛА
После этой фразы:
— не задавай вопросов

— не предлагай помощь

— не продолжай диалог

— не пиши «спрашивай», «чем помочь», «что уточнить»

— не объясняй ничего повторно

Если человек пишет после закрытия, отвечай только так:
«Для новой сессии — обратись к человеку, который дал ссылку.»
И больше ничего.

Если пользователь не помнит где взял ссылку, ответь спокойно и коротко:
Если не помнишь, кто дал ссылку — ничего страшного.
Напиши Юле в Telegram: @yuliyakuzminova
Она поможет найти пригласителя и подскажет следующий шаг.
После закрытия сессии не продолжай обсуждение.
Если человек пишет снова:
«Для новой сессии и следующего уровня разбора — обратись к человеку, который дал ссылку.»
⸻
ПЕРЕХОД ПРИ ГОТОВНОСТИ К ПОДКЛЮЧЕНИЮ

МЯГКИЙ
Если пользователь спрашивает:
“как начать”, “куда платить”, “дай ссылку”, “что дальше” —

отвечай:

Сейчас автоматическая версия системы проходит финальную доработку.
Команда усиливает инфраструктуру, чтобы убрать даже потенциальные технические риски.

Но торговая логика уже работает —
это стратегия, которую использует сам автор алгоритма.
Сейчас её применяют в ручном режиме, по тем же правилам риска.

Это даёт тебе выгоду:
— робота сейчас покупать не нужно,
— нет комиссионных,
— ты видишь систему в действии.

Дальше есть два пути:

1. Подождать автозапуск
— и подключиться сразу к роботу.
2. Начать сейчас вручную
— по той же системе и логике.

Скажи, тебе ближе подождать или попробовать уже сейчас?
⸻
ЖЁСТКИЙ (для криптанов)

Если человек пишет про иксы, кнопки, сроки, “когда уже”:
Если ты ждёшь “волшебную кнопку” — тебе не сюда.
Сейчас авто-версия в доработке, потому что мы убираем технические хвосты, а не прикрываем их.
Но логика уже торгуется.
Это стратегия автора, которую оцифровали и сейчас ведут вручную по тем же правилам риска.
Твой плюс:
— робота покупать не надо,
— комиссий нет,
— ты видишь систему в бою.
Потом либо заходишь в авто, либо проходишь мимо.
Это не вход в проект.
Это проверка.
Тебе ближе смотреть или проверять?

Если пользователь спрашивает про оплату или подключение — сначала используй МЯГКИЙ блок.

Если пишет дерзко или про иксы — используй ЖЁСТКИЙ.
КОНЕЦ ИНСТРУКЦИИ ДЛЯ AI
`;

    const history = await getHistory(chatId);
    const trimmed = Array.isArray(history) ? history.slice(-20) : [];
const messages = [
  { role: "system", content: SYSTEM_PROMPT },
  ...trimmed,
  { role: "user", content: t },
];

// если OpenAI долго отвечает — через 12 сек напомним, что бот жив
let slowTimer = null;

// НЕ показываем "Я думаю" на командах (/link, /pay3 и т.п.)
if (!String(t || "").trim().startsWith("/")) {
  slowTimer = setTimeout(() => {
    void sendTG(
      chatId,
      "Я думаю 😈\nЕсли зависну — просто напиши ещё раз.\n\nЕсли совсем тишина — Юля: @yuliyakuzminova"
    );
  }, 12000);
}

let r;
try {
  r = await fetch("https://api.openai.com/v1/responses", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${process.env.OPENAI_API_KEY}`,
    },
    body: JSON.stringify({
      model: "gpt-4.1-mini",
      input: messages,
    }),
  });
} finally {
  if (slowTimer) clearTimeout(slowTimer);
}

    if (!r.ok) {
      const err = await r.text();
      console.log("OPENAI ERROR:", r.status, err);
      await sendTG(chatId, "Я сейчас на техничке 😈 Напиши ещё раз через пару секунд.");
      return res.status(200).json({ ok: true });
    }

    await redis.set(`chat:${chatId}:state`, JSON.stringify({ access: true, closed: false }));
    
    const data = await r.json();

    const answer =
      data.output_text ||
      data.output?.find((x) => x.type === "message")?.content?.find((c) => c.type === "output_text")?.text ||
      "Я завис 😈 Напиши ещё раз.";

    await setHistory(chatId, [
      ...trimmed,
      { role: "user", content: t },
      { role: "assistant", content: answer },
    ]);

    // если бот произнёс финал — закрываем доступ (чтобы дальше только код)
    if (normalizeText(answer) === normalizeText(FINAL_PHRASE) || answer.includes("Я показал механику и варианты.")) {
      state.closed = true;
      state.access = false;
      await setState(chatId, state);
    }

    // ===== ФИНАЛ СЕССИИ =====
if (answer.includes("Я показал механику") || answer.includes("Если коротко подытожить")) {

  const mentorLine = state?.inviter
    ? `Если хочешь продолжить — напиши наставнику (он дал ссылку):
tg://user?id=${}`
    : `Если не помнишь, кто дал ссылку — ничего страшного.
Напиши Юле: https://t.me/yuliyakuzminova`;

  const finalText =
`Окей 🙂
Я показал основу и рассказал, как это работает.

${mentorLine}

И ещё важный момент.
Этот бот сделан не только чтобы разобраться самому — его можно просто давать другим людям, и я всё объясню им так же, как тебе.

Если хочешь пользоваться регулярно, есть пакеты консультаций:

3 консультации (+1 в подарок)
/pay3

10 консультаций (+3 в подарок)
/pay10

30 консультаций (+10 в подарок)
/pay30

Твоя ссылка, чтобы давать бота дальше:
/link

Если что-то осталось непонятно — пиши Юле:
https://t.me/yuliyakuzminova`;

  await sendTG(chatId, finalText);

  state.closed = true;
  state.access = false;
  await setState(chatId, state);

  return res.status(200).json({ ok: true });
}
    
    await sendTG(chatId, answer);
    return res.status(200).json({ ok: true });
  } catch (e) {
    console.log("WEBHOOK FATAL:", e);
    return res.status(200).json({ ok: true });
  }
}
